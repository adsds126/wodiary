spring:
  datasource:
    url: jdbc:mysql://wodiarydb.cl0kk4oq26md.ap-northeast-2.rds.amazonaws.com:3306/woDB
    username: admin
    password: ${db_pw}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql: TRACE
      org.springframework: DEBUG
